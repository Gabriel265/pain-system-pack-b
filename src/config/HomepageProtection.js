// src/config/HomepageProtection.js

// Lightweight guardrail to mark homepage as protected
export const isHomepageProtected = true; // Default to protected

// Function to unlock homepage for AI changes if needed
export const unlockHomepage = () => {
  // This function can be expanded to include logic to unlock the homepage
  // For now, it's a placeholder to indicate the pattern
};
